#include <stdio.h>

int main(void)
{
    unsigned i; // equivalent to unsigned int i;
    short j; // equivalent to short int j;
    signed x; // equivalent to signed int x;
    long long y; // equivalent to long long int y;

    printf("Implicit int is very much alive!\n");

    return 0;
}
