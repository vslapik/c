#include <stdio.h>

int main(int argc, char **argv)
{
    if (argc = 1)
        puts("No args");
    else if (argc = 2)
        puts("One arg");
    else if (argc = 3)
        puts("Two args");
    else
        puts("Many args");

    return 0;
}
